// JavaScript for interactive elements (e.g., carousel, form validation)
$(document).ready(function() {
    // Initialize carousel
    $('.carousel').carousel({
        interval: 2000
    });
});
